name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Test scripts
      run: |
        echo "Testing BD Termux Tools"
        find bin/ -type f -exec echo "Checking {}" \;